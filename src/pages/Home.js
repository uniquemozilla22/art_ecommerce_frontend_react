import React from "react";
import { Route, Routes } from "react-router";
import Layout from "../components/Layout";

const Home = () => {
  return (
    <Layout>
      <h2>Home</h2>
    </Layout>
  );
};

export default Home;
